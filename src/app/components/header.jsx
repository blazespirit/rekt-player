import React from 'react';
import Clock from './clock.jsx';

export default class App extends React.Component {
    render() {
        return (
            <div>
                <Clock />
            </div>
        );
    }
}