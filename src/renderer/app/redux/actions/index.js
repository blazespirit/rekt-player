export const changePage = (page) => ({
    type: 'CHANGE_PAGE',
    page: page
});

export const changeMenuFocusIndex = (focusIndex) => ({
    type: 'CHANGE_MENU_FOCUS_INDEX',
    focusIndex: focusIndex
});